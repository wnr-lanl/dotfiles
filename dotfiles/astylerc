# artistic style rc

# start with this style
--style=kr

# do char *foo and (char *)
--align-pointer=name

# use 4 spaces for indent
--indent=spaces=4
# convert tabs to spaces
--convert-tabs

# pad empty lines around header blocks (e.g. 'if', 'for', 'while')
--break-blocks=all

#Insert space padding after paren headers only (e.g. 'if', 'for', 'while'...).
--pad-header

# if () {
# }
# else {
# }
--break-closing-brackets

# for one liners
--add-brackets

#--align-reference=name
-k3

# don't do stuff like malloc (), do malloc()
--unpad-paren

# do i + j not i+j
--pad-oper

#--max-instatement-indent for line continuations
# like foo(bar,
#          buzz);
-M80

--min-conditional-indent=0

--indent-col1-comments
